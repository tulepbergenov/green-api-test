{
  "name": "green-api-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite --host --open",
    "build": "tsc && vite build",
    "preview": "vite preview --host --open"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.3.4"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  }
}
